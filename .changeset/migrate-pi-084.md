---
"@dustinbyrne/kb": patch
---

Migrate the engine, dashboard authentication, and extension integration to the `@earendil-works/pi` 0.84 SDK. Agent failures reported through assistant message state are now surfaced instead of being treated as successful runs.
